class Ship {
  constructor(length) {
    this.length = length;
    this.hit = 0;
  }

  // function hit() {
  //     Increases the hit property
  // }

  // function isSunk?() {
  //     if ship length is the same as ship hit, return true
  //     else return false
  // }
}

export { Ship };
